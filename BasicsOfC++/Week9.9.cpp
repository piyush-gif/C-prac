// Binary Search Tree

// left nodes for the lesser and right nodes for the greater than
#include<iostream>
// next day

struct Node {
	int data;
	Node* left;
	Node* right;
};


void add(Node*& root) {

}


int main() {
	Node* root = new Node{ 10, nullptr };

	add(root);
	return 0;
}